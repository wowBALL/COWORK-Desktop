# Changelog

## v1.9.3

**เพิ่มของใหม่ — แท็บ Redmine ค้นหางานได้แล้ว**

แท็บ Redmine กรองงานได้ 4 ทาง (ชิปโปรเจกต์ · ชิปผู้รับผิดชอบ · ระดับความเสี่ยง · แท็บสถานะ) แต่ทุกทาง
คือการ **กดเลือกจากค่าที่มีอยู่** ไม่มีทางไหนพิมพ์หาได้เลย ผลคืองานที่ปิดไปแล้วเข้าถึงได้ทางเดียว
คือกดไปแท็บ Closed แล้วไล่สายตาทีละ 15 แถว และคำอย่าง "voucher" ที่กระจายอยู่หลายโปรเจกต์ก็กรองไม่ได้

- เพิ่ม **ช่องค้นหา** ใต้หัวข้อ "งาน Redmine" — ค้นจาก เลข # / ชื่อเรื่อง / โปรเจกต์ / ผู้รับผิดชอบ และ
  **โน้ตส่วนตัว** ที่จดไว้ในเครื่อง หลายคำคั่นด้วยช่องว่างต้องตรงทุกคำ ไม่สนตัวพิมพ์เล็กใหญ่
  พิมพ์ `550` หรือ `#550` ก็เจออันเดียวกัน · กด **Esc** ล้างช่อง
- **ค้นข้ามทุกสถานะ รวมงานที่ปิดแล้ว** ไม่ต้องรู้ก่อนว่างานอยู่สถานะไหน ผลเรียงงานที่ยังเปิดขึ้นก่อน
  แล้วเรียงตามเวลาที่แก้ไขล่าสุด ชิปกับแท็บสถานะยังกดกรองซ้อนคำค้นได้เหมือนเดิม
- ถ้ายืนอยู่แท็บสถานะเจาะจงแล้วไม่เจอ จะบอกว่า **มีอีกกี่งานที่ตรงอยู่ในสถานะอื่น** แทนที่จะขึ้นว่าไม่พบเฉย ๆ
- แถวที่โผล่มาเพราะคำค้นไปตรงกับ**โน้ต** ปุ่ม 📝 จะติดไฮไลต์และบอกใน tooltip พร้อมตัวอย่างโน้ต
  จะได้ไม่งงว่าแถวนี้มาได้ยังไง
- **ตอนช่องค้นหาว่าง ทุกอย่างเหมือนเดิมทุกประการ** ทั้งตัวเลข ลำดับ และแท็บ ALL ที่ยังตัด Closed/Backlog ออก

**แก้ของเสียที่เจอระหว่างทำ**

- tooltip เวลาสร้าง/แก้ไขของแต่ละแถว เคยบังชื่อเรื่องของแถวบน**ทั้งบรรทัด** ย้ายไปชิดขวาแล้ว
  เหลือบังแค่หาง `…` 12px ส่วนที่โดนทับแทนคือป้ายโปรเจกต์กับชื่อคนซึ่งซ้ำกันแทบทุกแถว
- ชิปที่ไม่มีผลตอนค้นอยู่ ตั้งใจให้จางลง แต่จางเกินจนอ่านไม่ออกจริง ๆ (วัดได้ 1.84:1 บนธีมสว่าง)
  ปรับให้ผ่าน 3:1 ทั้งสองธีม

**ข้างในที่ไม่เห็นจากหน้าจอ**

การคิดตัวเลขทั้งแท็บ (ตัวเลขบนชิป แถบความเสี่ยง แท็บสถานะ และรายการที่แสดง) ย้ายออกจากโค้ดที่วาดจอ
มารวมอยู่ในฟังก์ชันเดียวที่เทสได้โดยไม่ต้องมีหน้าจอ อาการ "เลขบนแท็บไม่ตรงกับจำนวนแถวข้างล่าง"
ซึ่งเคยเกิดกับตัวกรองของวิดเจ็ตนี้มาแล้วสามครั้ง จึงเป็นไปไม่ได้เชิงโครงสร้าง ไม่ใช่แค่เพราะเขียนระวัง

## v1.9.2

**เพิ่มของใหม่ — แท็บ Meeting เห็นจุดที่ควรตรวจสรุปได้แล้ว**

ตัวอัดประชุมเขียนไฟล์ `summary.meta.md` แยกจาก `summary.md` มาสักพักแล้ว (โมเดลที่ใช้สรุป
ประเภทประชุม คำที่แก้ตาม glossary คำที่โมเดลเดาแล้วอาจผิด และช่วงเวลาที่เสียงไม่ชัดควรฟังซ้ำ)
แต่วิดเจ็ตไม่เคยอ่านไฟล์นี้เลย ต้องเปิดโฟลเดอร์ประชุมด้วยโปรแกรมอื่นถึงจะเห็น

- เพิ่มแท็บ **⚠ ที่ต้องเช็ก** ในหน้าอ่านประชุม ต่อจาก สรุป/ถอดเสียง/ไฟล์ — โชว์คำที่น่าจะถอดเพี้ยน
  แบบ **ได้ยิน → เดาว่า** พร้อมจำนวนครั้ง และช่วงเวลาที่ควรฟังซ้ำแยกป้ายเวลาออกจากเนื้อหาให้กวาดตาหาไว
- **แท็บนี้โผล่เฉพาะประชุมที่มีไฟล์ `summary.meta.md`** ประชุมเก่าก่อนตัวอัดเริ่มเขียนไฟล์นี้
  (ตอนนี้คือ 7 ใน 9 ประชุม) จะไม่มีแท็บนี้เลย ไม่ใช่แท็บที่กดแล้วเจอหน้าว่าง

## v1.9.1

**แก้ของเสีย — v1.9.0 ที่ปล่อยไปเปิดไม่ขึ้น**

ตัวติดตั้ง v1.9.0 ตายทันทีที่เปิด ขึ้นกล่อง `A JavaScript error occurred in the main process` /
`Cannot find module './grafana'` และเข้าแอปไม่ได้เลย ไม่ใช่แค่แท็บ Grafana พัง

- สาเหตุ: `build.files` ใน `package.json` เป็น **allowlist** ไฟล์ที่ไม่ได้ใส่ชื่อจะไม่ถูกแพ็กเข้า `app.asar`
  ทั้ง `grafana.js` `tab-grafana.js` `tab-grafana.css` ตกไปทั้งสามตัว
  ตอนรันจากซอร์สอ่านจากดิสก์ตรง ๆ ทุกอย่างจึงทำงานปกติ — ความพลาดแบบนี้โผล่เฉพาะในตัวติดตั้ง
- เพิ่มเทส `tests/packaged-files.test.js` กันซ้ำ: ทุกไฟล์ที่ `widget.html` โหลด และทุกโมดูลที่
  `main.js` / `preload.js` `require` (ไล่ต่อเป็นทอด ๆ) ต้องอยู่ใน `build.files` และต้องมีไฟล์อยู่จริง
  พร้อมเช็คว่า `CHANGELOG.md` มีหัวข้อของเวอร์ชันใน `package.json`

**ถ้าลง v1.9.0 ไปแล้ว** ให้ลงตัวนี้ทับได้เลย ไม่ต้องถอนก่อน

## v1.9.0

**เพิ่มของใหม่ — แท็บ Grafana**

ดู log จาก **Loki** ที่อยู่หลัง Grafana ที่ตั้งค่าไว้ แยก **Dev / Prod** เป็นสอง instance คนละ token
ต้องตั้งค่าก่อนใช้: เฟือง ⚙ → การ์ด **Grafana** → ใส่ URL กับ API token สิทธิ์ **Viewer**
(หา token ที่ Grafana → ⚙ Configuration → **Service accounts** หรือ **API keys** — Grafana 9.x เมนูนี้อยู่ใต้ Configuration ไม่ใช่ Administration) · **Prod ยังไม่ได้ตั้งค่า** ปุ่มจะจางไว้จนกรอกค่า

- **2 โหมด** — *เฉพาะปัญหา* (ค่าเริ่มต้น) ให้ Loki กรองเฉพาะบรรทัดที่เป็นปัญหาให้ตั้งแต่ต้นทาง · *ทุกบรรทัด* เห็นทุกอย่าง
  ที่ปริมาณ log ของคลัสเตอร์ (24 ชม. มีราว 650,000 บรรทัด) โหมดทุกบรรทัดครอบได้แค่ช่วงสั้น ๆ — มีบรรทัดใต้ตัวกรองบอกตรง ๆ ว่ากำลังเห็นกี่บรรทัดจากทั้งหมดเท่าไร และครอบช่วงเวลาจริงถึงไหน
- **ช่วงเวลา** 15m · 30m · 1h · 1 วัน · 2 วัน · 3 วัน · **กำหนดเอง** (ระบุวันเวลาเริ่ม–สิ้นสุดเอง)
- **รวมที่ซ้ำกัน** — error เดิมที่เกิดซ้ำยุบเป็นแถวเดียว พร้อมจำนวน จังหวะการเกิด (เช่น "ทุก ~1 นาที") แถบความถี่รายช่วง และ % เทียบกับบรรทัดทั้งหมดของ app นั้น · สลับไป *ไล่ตามเวลา* ได้
- **ป้ายอายุปัญหา** 🆕 ใหม่วันนี้ / ⏳ ทุกวัน N วัน / เกิด N/8 วัน — มาจากการนับย้อนหลัง 8 วันจริง และเรียงให้ **ของใหม่ขึ้นก่อนของเรื้อรัง**
  จำนวนครั้งบอกไม่ได้ว่าควรไล่ตัวไหนก่อน: cron ที่พังมาแปดวันได้ 1,537 ครั้ง/วัน แต่บั๊กที่เพิ่งเกิดวันนี้ได้ 2 ครั้ง — ตัวหลังคือตัวที่เพิ่งมีคนทำพัง
- **แยก error ตามชั้น** Error (ของแอป) · Error 5xx · Error 4xx · **Error 2xx** — ตัวสุดท้ายคือ "ปลายทางตอบ OK แต่งานล้ม" เช่น Odoo ตอบ HTTP 200 มาพร้อม error body ซึ่งเกิดเป็นหมื่นครั้งต่อสัปดาห์ ถ้ากองรวมใน Error ก้อนเดียวจะมองไม่เห็นเลย
- **ค้นหา** ข้อความ + ชื่อ app / pod / container / namespace · หลายคำ = ต้องมีทุกคำ · `"ในอัญประกาศ"` = ต้องติดกันเป๊ะ
  ถ้าคำที่ค้นมีอยู่จริงแต่ถูกตัวกรองอื่นซ่อนไว้ จะมี **แถบเตือนพร้อมปุ่มคลายตัวกรองให้** — ไม่ปล่อยให้ 0 ผลลัพธ์ถูกอ่านว่า "ไม่มี log แบบนี้"
- **Endpoint ที่ช้าสุด** p50 / p95 / จำนวนคำขอ — คำขอที่สำเร็จแต่ช้า ซึ่งหน้า error มองไม่เห็นเลย · แถวที่คำขอน้อยเกินจะเชื่อ p95 มีคำเตือนกำกับ
- ปุ่ม ↻ กดโหลดใหม่ได้ และแท็บดึงเองทุก 1 นาที **เฉพาะตอนเปิดแท็บอยู่**

**สิ่งที่ควรรู้เกี่ยวกับตัวเลขที่เห็น**
- ช่วงที่ยาวกว่า 1 ชั่วโมงจะถูก **สุ่มกระจาย 10 ช่วงย่อย ละ 12 นาที ทั่วช่วงที่เลือก** ไม่ใช่เอาแต่บรรทัดล่าสุด — ถ้าเอาแต่บรรทัดล่าสุด ทุกช่วงเวลาจะได้ข้อมูลชุดเดียวกันราวหกนาทีท้าย ๆ และการเปลี่ยนช่วงเวลาจะไม่มีผลอะไรเลย
- **ตัวเลขในแถบสรุปเป็นยอดจริงของทั้งช่วงที่นับฝั่ง Loki** ไม่ใช่นับจากตัวอย่างที่ดึงมา (ถ้านับจากตัวอย่าง เลข error จะ *น้อยลง* เมื่อขยายช่วงเวลา ซึ่งกลับหัวกับความจริง) — มีคำกำกับใต้แถบบอกไว้ทุกครั้งว่าเลขมาจากไหน
- ระหว่างค้นจะมีแถบบอกว่ากำลังหาอะไรอยู่ และตัวกรองถูกกันไม่ให้กดซ้ำ เพราะเลขบนชิปยังเป็นของชุดเก่า

**ข้อจำกัด**
- ทดสอบกับ Grafana 9.3.8 + Loki 2.6.1 · ต้องมี datasource ชนิด Loki (หน้าตั้งค่ามีปุ่มทดสอบบอกให้ว่าเจอหรือไม่)
- ช่วง 3 วันใช้เวลาค้นสองสามวินาที ส่วน 15 นาที–1 ชั่วโมงได้ผลแทบทันที
- token เก็บอยู่ฝั่ง main process เท่านั้น หน้าจอเห็นแค่ว่ามีหรือยังกับสี่ตัวท้าย

## v1.8.9

**เพิ่มของใหม่**
- **แท็บ Meeting เลือกประเภทประชุมได้แล้ว ไม่ใช่แค่โมเดล** — กดปุ่ม `⋯` ข้างปุ่มเปิดห้อง จะมีให้เลือกสองแบบใต้รายการโมเดล: **dev ล้วน** (ศัพท์เทคนิคตรงๆ) และ **Business + dev** (แยก "ทำได้" ออกจาก "จะทำ" · ขยายศัพท์ให้คนนอกทีม) เหมือนที่หน้าเว็บของตัวอัดมีอยู่แล้ว ก่อนหน้านี้ใครอยากได้สรุปแบบ Business + dev ต้องไปเปิดห้องจากหน้าเว็บแทน ทั้งที่วิดเจ็ตทำอย่างอื่นได้ครบแล้ว
  - **จำค่าที่เลือกไว้ครั้งล่าสุด** เปิดห้องครั้งถัดไปกดปุ่มเดียวได้เลย ค่าเริ่มต้นคือ dev ล้วน ซึ่งเป็นค่าเดิมที่ใช้มาตลอด — ไม่ตั้งค่าอะไรเลยก็ได้ผลเหมือนเดิมทุกอย่าง
  - **โหมดถอดเสียงอย่างเดียวจะไม่ถามประเภทประชุม** เพราะไม่มีขั้นสรุปให้ใช้กฎนั้นเลย ถามไปก็เป็นคำถามที่คำตอบไม่เปลี่ยนอะไร
  - แผง `⋯` ไม่ปิดตัวเองหลังกดเลือกอีกแล้ว — ตอนนี้มีสองอย่างให้ตั้งในแผงเดียว ปิดทันทีเท่ากับบังคับให้กดเปิดแผงใหม่เพื่อเลือกอย่างที่สอง กดปุ่ม `⋯` ซ้ำเพื่อปิด
  - ระหว่างอัดอยู่ แถบจะบอกแค่ชื่อห้องกับโมเดลตามเดิม **ไม่โชว์ประเภทประชุม** เพราะตัวอัดไม่ได้ส่งค่านั้นกลับมา ห้องที่กำลังอัดอาจถูกเปิดจากหน้าเว็บด้วยประเภทอื่น การเดาแล้วขึ้นป้ายผิดแย่กว่าไม่มีป้าย

**ต้องอัปเดต meeting-notes ด้วย ไม่งั้นตัวเลือกนี้ยังไม่มีผล**
- ระบบประเภทประชุมอยู่ฝั่ง meeting-notes เครื่องที่ยังไม่ได้ดึงโค้ดใหม่จะกดเลือกได้ แต่สรุปยังออกมาแบบ dev ล้วนทุกครั้ง
- และฝั่งนั้นเคยมีบั๊กที่ทำให้ประเภทประชุม **ไม่เคยมีผลเลย** ไม่ว่าจะเลือกจากวิดเจ็ตหรือจากหน้าเว็บของมันเอง: ตัวอัดเขียนไฟล์กำกับทับตอนปิดไฟล์เสียง แล้วค่าที่เลือกไว้หายไปเงียบ ๆ ซึ่งเกิดกับทุกการอัดเพราะทุกการอัดปิดไฟล์เสียงตอนหยุด เจอตอนทดสอบฟีเจอร์นี้ แก้แล้วและอยู่ใน `main` ของ meeting-notes เรียบร้อย
- **วิธีอัปเดต:** `git pull` ที่โฟลเดอร์ meeting-notes แล้วปิด-เปิด `start-ui.bat` ใหม่

## v1.8.8

**เปลี่ยนวิธีทำงาน** — รอบนี้กลับคำสิ่งที่ v1.8.7 ประกาศไว้ โปรแกรมไม่เป็นเจ้าของตัวอัดประชุมอีกแล้ว

- **ต้องเปิด `start-ui.bat` ที่ meeting-notes เองก่อน** แล้วจึงกดอัดจากแท็บ Meeting ได้ — โปรแกรมไม่เรียกตัวอัดขึ้นมาให้เองอีก การเรียกให้เองคือต้นเหตุของทั้งหน้าต่างดำที่โผล่ขึ้นมาเองและตัวที่ค้างกินเครื่อง ถ้ายังไม่ได้เปิด แถบจะบอกไว้ให้
- **ปิดโปรแกรมระหว่างอัดได้เลย ไม่ถามอะไร และการอัดยังเดินต่อ** — กล่องที่ v1.8.7 เพิ่มมา (ปิดทั้งหมด / ปิดแค่หน้าต่าง / ยกเลิก) ถูกถอดออกทั้งกล่อง เพราะการอัดเป็นของ meeting-notes ไม่ใช่ของโปรแกรมนี้ ผลพลอยได้ที่สำคัญกว่า: โปรแกรมนี้ไม่มีทางไปหยุดการอัดที่กำลังทำงานได้อีกเลย ซึ่งเป็นความเสียหายที่กล่องนั้นถูกสร้างมากันตั้งแต่แรก
- **แถบสถานะเล่าเป็นขั้นแบบห้องส่ง** — `OFF AIR` (ยังไม่ได้เปิดตัวอัด) → `รอความพร้อม ก่อน On Air…` (ติดต่อได้แล้ว กำลังถามสถานะ ยังไม่ให้กดเปิดห้อง) → `STANDBY` (พร้อม กดเปิดห้องได้) → `ON AIR` (กำลังบันทึกเสียงอยู่จริง)
  - **`ON AIR` ขึ้นเฉพาะตอนเสียงกำลังเข้าเท่านั้น** กดปิดห้องแล้วจะเปลี่ยนเป็น `กำลังปิด` ทันที และค้างอยู่อย่างนั้นตลอดช่วงที่ยังบีบอัดไฟล์ — ยาวตามความยาวประชุม (วัดจริง: ประชุม 2 นาทีครึ่งใช้เวลา 1 วินาที) ไฟแดงจึงหมายถึงสิ่งเดียวกับที่ทุกคนเข้าใจ

**แก้บั๊ก**
- **ปุ่มอัดหายไปเมื่อตัวอัดตอบช้า** — เดิมโปรแกรมวัดว่า "มีตัวอัดอยู่ไหม" ด้วยคำถามเดียวกับที่ใช้อ่านสถานะ ซึ่งฝั่งนั้นต้องไปสำรวจโพรเซสทั้งเครื่องก่อนตอบ (วัดได้ 0.4–2.1 วินาที) เกินเวลาที่โปรแกรมยอมรอ ผลคือแถบขึ้นว่ารอตัวรันประชุมอยู่ และ **ปุ่มอัดหายไปค้างอย่างนั้น** ทั้งที่ตัวอัดรันอยู่ปกติ ตอนนี้แยกเป็นสองคำถาม และถ้าอ่านสถานะได้ก็ถือว่าติดต่อได้ทันที ไม่ว่าคำถามแรกจะตอบทันหรือไม่

**สิ่งที่ต้องทำครั้งเดียว — เฉพาะเครื่องที่เคยรัน v1.8.7**
- ตัวอัด (`session_service`) ที่ v1.8.7 เปิดไว้เองไม่มีหน้าต่างให้กดปิด และหลังอัปเดตแล้วจะไม่มีใครไปปิดมันอีกเลย มันยังถือพอร์ต 8765 อยู่ ทำให้ `start-ui.bat` เปิดไม่ขึ้นเพราะจองพอร์ตไม่ได้
- วิธีจัดการ: เปิด `http://127.0.0.1:8765/api/state` ในเบราว์เซอร์ ดูค่า `recorder` ก่อน — **ถ้ากำลังอัดอยู่ ห้ามปิด** ให้ปิดห้องให้เรียบร้อยก่อน แล้วจึงปิดโพรเซสที่ command line มีคำว่า `*src.session_service*` (หาจาก command line ไม่ใช่จำเลข pid — เลขเปลี่ยนทุกครั้งที่รีสตาร์ต) จากนั้นเปิด `start-ui.bat` ใหม่
- ถ้าใช้พอร์ตอื่น ให้ดูค่าที่ตั้งไว้ในหน้าตั้งค่า → การ์ด Meeting

## v1.8.7

**แก้บั๊ก**
- **ตัวประมวลผลประชุมค้างอยู่หลังปิดโปรแกรม** — โปรแกรมเป็นคนเปิดมันขึ้นมาเอง แต่ไม่เคยปิดให้ มันจึงค้างกินเครื่องอยู่อย่างนั้นและสะสมเพิ่มทุกครั้งที่เปิดโปรแกรมใหม่ ตอนนี้ปิดโปรแกรมแล้วปิดให้ด้วย รวมถึงตัวที่ค้างมาจากรอบก่อน ๆ ซึ่งโปรแกรมจะไปหาเจอตอนเปิดครั้งถัดไปแล้วเก็บกวาดให้ตอนปิด
  - ถ้าตอนนั้นยังอัดเสียงหรือประมวลผลอยู่ จะถามก่อนเสมอ เลือกได้ว่าจะปิดทั้งหมด ปิดแค่หน้าต่างโปรแกรมแล้วปล่อยให้อัดต่อ หรือยกเลิกไม่ปิดอะไรเลย
- **หน้าต่างดำโผล่ขึ้นมาเอง** — ตอนโปรแกรมเปิดตัวประมวลผลประชุม จะมีหน้าต่างคอนโซลดำ ๆ เด้งขึ้นมาค้างไว้ ตอนนี้ไม่โผล่แล้ว

## v1.8.6

**แก้บั๊ก**
- **อัปเดตเสร็จแล้วโปรแกรมไม่เปิดคืนให้** — กด "รีสตาร์ทตอนนี้" แล้วโปรแกรมปิดไปเฉย ๆ ต้องไปกดไอคอนเปิดเอง ทั้งที่ปุ่มบอกว่าจะรีสตาร์ทให้ สาเหตุคือไม่เคยสั่งตัวติดตั้งให้เปิดแอปคืนเลยสักครั้ง ตอนนี้สั่งแล้ว
  - **เริ่มมีผลตั้งแต่การอัปเดตครั้งถัดจากรอบนี้ไป** เช่นเดียวกับตัวแก้ใน v1.8.5 เพราะการอัปเดตทำโดยตัวที่ติดตั้งอยู่เดิม รอบนี้จึงยังต้องเปิดเองอีกครั้งหนึ่ง

## v1.8.5

**แก้บั๊ก**
- **อัปเดตอัตโนมัติที่ไม่ได้อัปเดตจริง** — กด "รีสตาร์ทตอนนี้" แล้วโปรแกรมปิดไปเหมือนปกติ แต่บางครั้งไม่ได้ติดตั้งอะไรเลยสักอย่าง ทั้งไฟล์และข้อมูลเวอร์ชัน เปิดมาใหม่ก็ยังเป็นเวอร์ชันเดิมและเด้งบอกว่ามีของใหม่ซ้ำอีก — ไม่มีข้อความบอกเลยสักบรรทัด สาเหตุคือตัวติดตั้งเริ่มทำงานตอนโปรแกรมยังปิดไม่สนิท เลยลบไฟล์ชุดเก่าไม่ได้แล้วยกเลิกทั้งหมด ตอนนี้รอให้ปิดสนิทก่อนเสมอ
  - ผลพลอยได้: เวอร์ชันที่โชว์ในหน้า "แอปที่ติดตั้ง" ของ Windows จะตรงกับความจริงแล้ว ของเดิมค้างอยู่ที่ 1.3.15 มาตั้งแต่ 24 ก.ค.
  - **ตัวแก้นี้จะเริ่มทำงานตั้งแต่การอัปเดตครั้งถัดจากรอบนี้ไป** เพราะการอัปเดตทำโดยตัวที่ติดตั้งอยู่เดิม
- **เปิดโปรแกรมซ้ำได้เป็นสองตัวซ้อนกัน** — กดไอคอนตอนที่เปิดอยู่แล้วจะได้หน้าต่างใหม่มาทับของเดิมพอดีจนดูไม่ออกว่ามีสองตัว และตัวที่สองจะลืมธีมที่ตั้งไว้ กลับไปเป็นสีเข้มเสมอ ตอนนี้กดซ้ำจะดึงหน้าต่างเดิมขึ้นมาแทน (ถ้าย่อไว้ก็คืนให้)

## v1.8.4

**แก้บั๊ก**
- **QA test: หน้าอ่านค้างโชว์รันเก่าเมื่ออ่านโฟลเดอร์ไม่ได้** — ถ้ากำลังเปิดรันอยู่แล้วรีเฟรชเจอปัญหา (โฟลเดอร์ถูกย้าย ไดรฟ์หลุด ตั้งค่าผิด) หน้าจะค้างโชว์รันที่ไม่มีอยู่แล้ว ส่วนข้อความ "ตั้งค่าเลย" ไปขึ้นอยู่ใต้รายการที่ถูกซ่อนไว้ ตอนนี้ล้างหน้าอ่านแล้วพากลับไปหน้ารายการให้เอง เป็นบั๊กที่มีมาตั้งแต่ทำแท็บนี้
- **Meeting: ชื่อห้องที่มีเครื่องหมาย `"` ขาดกลางคัน** — พิมพ์ `ทดสอบ "รอบ 2"` ไว้ในช่องชื่อห้อง พอแถบวาดใหม่ (เช่นตอนตัวประมวลผลเปลี่ยนสถานะ) ข้อความจะเหลือแค่ `ทดสอบ ` ตอนนี้เก็บครบทุกตัวอักษร

**เปลี่ยนที่เห็นได้ (เฉพาะบางเครื่อง)**
- **ป้ายชื่อ source ใน QA test ที่มีอิโมจิจะเปลี่ยนสีไปหนึ่งครั้ง** — สีมาจากการหมุน 8 สีตามชื่อ ซึ่งย้ายมาใช้ตัวคำนวณกลางตัวเดียวกับที่อื่นแล้ว ชื่อที่ไม่มีอิโมจิได้สีเดิมทุกชื่อ (ตรวจแล้ว 83,487 ค่า ไม่มีตัวไหนเปลี่ยน)

**เปลี่ยนแปลงภายใน**
- เพิ่มเทสให้ตัวกรอง ปี/เดือน/วัน 24 ตัว ครอบกฎที่เคยเป็นบั๊กมาแล้วสองข้อ (การล้างตัวกรองที่ค้างอยู่ และการนับเลขบนชิป)
- ยุบตัวช่วยที่ยังเหลือซ้ำให้เหลือตัวเดียว (ตัวจัดการอักขระพิเศษ, ตัวแจกสีจากชื่อ) — สำเนาที่ต่างกันเล็กน้อยคือต้นเหตุของบั๊กชื่อห้องข้างบน

## v1.8.3

**เปลี่ยนแปลงภายใน** — รอบนี้เกือบทั้งหมดเป็นการรื้อโครงสร้าง หน้าตาและการใช้งานเหมือน v1.8.2 ทุกประการ

- **`widget.html` แยกเป็นเปลือก + หนึ่งไฟล์ต่อหนึ่งแท็บ** จาก 2,057 บรรทัดเหลือ 562 (27%) แก้แท็บไหนก็เปิดไฟล์นั้นไฟล์เดียว ไม่ต้องเลื่อนหาในไฟล์ยาว และไม่มีทางไปโดนแท็บอื่นโดยไม่ตั้งใจ
  - `tab-redmine` · `tab-workspace` · `tab-qatest` · `tab-meeting` (ตัวไหนมี CSS เยอะก็แยก `.css` ตามไปด้วย)
  - ของใช้ร่วมอยู่ที่ `util.js` (ตัวช่วยจัดรูปแบบ) และ `datefilter.js` (ตัวกรอง ปี/เดือน/วัน ที่ Meeting กับ QA test ใช้ด้วยกัน)
  - เปลือกเหลือหน้าที่: นาฬิกา ปุ่มหน้าต่าง ธีม ทะเบียนแท็บ การสลับแท็บ และหน้าตั้งค่า
- **เพิ่มแท็บใหม่ในอนาคตทำได้โดยไม่ต้องแก้กลไกกลาง** — เพิ่มไฟล์ + เพิ่มชื่อในทะเบียน + เพิ่ม markup ของ view จบ ลำดับ mount, การสลับแท็บ, การสลับธีม และลำดับการ์ดตั้งค่า อ่านจากทะเบียนเดียวกันหมด
- ยุบตัวช่วยที่มีหลายสำเนาให้เหลือตัวเดียว (ตัวจัดการอักขระพิเศษ, ตัวแจกสีจากชื่อ, ชื่อเดือนภาษาไทย) — สำเนาที่ต่างกันเล็กน้อยเคยทำให้เกิดบั๊กมาแล้ว

**เปลี่ยนที่เห็นได้ (จุดเดียว)**
- **การ์ดในหน้าตั้งค่าเรียงตามลำดับแท็บแล้ว** — เดิมเรียง Redmine → Workspace → Meeting → QA ซึ่งสลับที่กับแถบแท็บด้านบนที่เรียง Redmine → Workspace → QA test → Meeting

## v1.8.2

**เพิ่ม**
- **เปิด/ปิดห้องประชุมได้จากแท็บ Meeting แล้ว** — ต่อกับตัวรันของ meeting-notes ผ่าน `127.0.0.1` ไม่ต้องสลับไปเปิด `start-ui.bat` อีกหน้าต่าง เปิดห้องเป็นคลิกเดียวเพราะจำโมเดลสรุปที่ใช้ล่าสุดไว้ให้ (กด `⋯` เพื่อเปลี่ยน)
  - **เครื่องที่ไม่มีตัวรันจะไม่เห็นแถบนี้เลย** หน้าตาเหมือนเดิมทุกประการ ไม่ใช่ปุ่มเทาที่กดแล้วพัง
  - ปิดห้องต้องยืนยันทุกครั้ง เพราะประชุมอัดซ้ำไม่ได้
  - ตัวประมวลผลไม่พร้อมจะขึ้นเตือน แต่ยังกดอัดได้ ไฟล์รอในคิวไว้ก่อน — การอัดไม่ได้พึ่ง GPU
- **เห็นว่าประมวลผลถึงขั้นไหนแล้ว** ครบ 5 ขั้น (บีบอัดไฟล์เสียง → ถอดเสียง → แยกผู้พูด → สรุป → เสร็จ) กางดู log ของตัวประมวลผลได้ในตัว · โหมดถอดเสียงอย่างเดียวจะไม่แสดงขั้น "สรุป" · ถ้าล้มจะบอกว่าล้มที่ขั้นไหนและย้ำว่าไฟล์ยังอยู่ครบ ประชุมไม่ได้หาย
- **ตัวชี้สถานะการอัดบนแถบหัวข้อ** เห็นได้จากทุกแท็บ ไม่ใช่แค่แท็บ Meeting กดแล้วเด้งกลับมาที่แท็บ Meeting (ไม่มีปุ่มปิดห้องตรงนั้น กันกดพลาด)
- **ปุ่ม "อ่านสรุป"** พาเข้าหน้าอ่านของประชุมนั้นทันทีที่ประมวลผลเสร็จ ปิดวงจร อัด → ประมวลผล → อ่าน ในหน้าต่างเดียว
- ตั้งพอร์ตของตัวรันได้ที่หน้าตั้งค่า → การ์ด Meeting (ต้องตรงกับ `UI_PORT` ใน `.env` ของ meeting-notes)
- **ไม่ต้องเปิด `start-ui.bat` เองแล้ว** — widget เรียก `session_service` ขึ้นมาให้เองถ้ายังไม่รัน (เฉพาะเครื่องที่ตั้ง path meeting-notes ไว้และมี venv จริง) เปิดแค่ตัวรับคำสั่ง **ไม่เปิดตัวประมวลผล** เพราะมันโหลดโมเดลลง VRAM และ widget เปิดเองตอน login อยู่แล้ว
  - **ไม่ปิดให้ตอนปิด widget โดยตั้งใจ** — ปิดหน้าต่างระหว่างประชุมต้องไม่ทำให้การอัดหยุด ซึ่งเป็นเหตุผลที่ตัวรันแยกเป็นคนละ process ตั้งแต่แรก
  - ระหว่างที่ยังติดต่อไม่ได้จะขึ้นแถบ **"รอตัวรันประชุม…"** แทนที่จะเงียบไปเฉย ๆ — แต่เฉพาะเครื่องที่เคยต่อติดมาก่อน เครื่องที่ไม่เคยมีตัวอัดยังเห็นแท็บเหมือน v1.8.1 ทุกประการ
  - กดเข้าแท็บ Meeting แล้วถามสถานะทันที ไม่ต้องรอรอบตรวจถัดไป

**เปลี่ยนแปลงภายใน**
- โค้ดของแถบควบคุมแยกออกเป็น `meetingrun.js` / `meetingrun.css` แทนที่จะต่อท้าย `widget.html` ที่ยาว 1,993 บรรทัดอยู่แล้ว ใช้สัญญาโมดูล `mount / onData / onShow / onHide` ที่จะใช้รื้อทั้งไฟล์ใน v1.8.3
- เพิ่มชุดทดสอบหน่วยชุดแรกของโปรเจกต์ (`npm test` ด้วย `node --test` ไม่เพิ่ม dependency)

## v1.6.0

**เพิ่ม**
- **จดโน้ตส่วนตัวที่งานได้แล้ว** — ปุ่ม 📝 ท้ายทุกแถวงาน กดแล้วเขียนโน้ตกางลงมาใต้แถวนั้นทันที (ใช้กลไกเดียวกับปุ่มปิดงาน ✓) เก็บไว้บนเครื่องเท่านั้น **ไม่ถูกส่งไป Redmine ไม่ว่ากรณีใด** เหมาะไว้จดสิ่งที่จะถามกลับตอนงานมีปัญหา
  - แถวที่มีโน้ตแล้วปุ่มจะเปลี่ยนเป็นสีม่วงพร้อมจุดแจ้งเตือน เอาเมาส์ชี้ดูตัวอย่างโน้ตได้โดยไม่ต้องกด
  - แก้ไขค้างไว้แล้วปิดแผงโดยยังไม่บันทึก ระบบจะถามยืนยันก่อนทิ้งข้อความ กัน note หายโดยไม่ตั้งใจ
  - พองานถูกปิด (Closed) โน้ตของงานนั้นจะถูกลบทิ้งอัตโนมัติในรอบรีเฟรชถัดไป — ไม่มีการเตือนก่อนลบ เพราะประโยชน์ของโน้ตหมดไปตั้งแต่งานปิดแล้ว

## v1.5.0

**เพิ่ม**
- **เลือกได้แล้วว่าจะใช้ Dark หรือ Light Mode** — ปุ่ม ☀ / 🌙 อยู่บนแถบหัวข้อข้าง ๆ ปุ่มตั้งค่า กดทีเดียวสลับทันที ไม่ต้องเข้าหน้าตั้งค่า และจำค่าไว้ให้ตอนเปิดแอปครั้งหน้า
  - **Dark · Carbon** — พื้นกราไฟต์เทาเข้ม ดำลึกกว่าธีมเดิม ตัวหนังสือคมขึ้น สีหลักเป็นเขียวน้ำทะเล
  - **Light · Warm Sand** — พื้นครีมอุ่น ไม่ขาวจ้าแสบตา สีหลักเป็นเขียวหัวเป็ด
- ทุกสีในแอปผ่านการวัดค่าความต่างของสี (contrast) มาแล้วทั้งสองธีม ตัวหนังสือหลักอยู่ที่ 13.9–15.8 เท่า และทุกสีที่สื่อความหมาย (เกินกำหนด / ความเสี่ยง / สถานะ) ผ่านเกณฑ์ WCAG AA ทั้งหมด

**เปลี่ยนแปลงภายใน**
- รวมสีทั้งแอปให้มาจากตัวแปรชุดเดียว เดิมมีสีที่เขียนตายตัวกระจายอยู่ราว 60 จุด (เช่นสีตัวหนังสือบนปุ่ม สีป้ายโปรเจกต์ สีผู้พูดในบทถอดเสียง) ซึ่งพอเปลี่ยนเป็นพื้นสว่างแล้วจะอ่านไม่ออก — ตอนนี้เพิ่มธีมใหม่ทำได้โดยแก้ที่เดียว
- เพิ่มเฉดสี `--orange` และ `--mint` ให้ครบ 8 เฉด รองรับสีผู้พูด 8 คนและระดับความเสี่ยง 5 ขั้นโดยไม่ต้องเขียนสีตายตัวอีก

## v1.4.4

**เพิ่ม**
- หัวข้อย่อยในสรุปประชุม (เช่น "5. Attendant App") เดิมเป็นสีขาวธรรมดา กลืนกับตัวหนังสือรอบข้าง แยกยาก — ตอนนี้มีสีฟ้า (cyan) + แถบซ้าย + พื้นหลังจาง ๆ ให้กวาดตาเจอหัวข้อได้ทันที
- เนื้อหาสรุป + สารบัญ ตัวอักษรใหญ่ขึ้นอัตโนมัติตอนขยายเต็มจอ (จอกว้างพอ) อ่านสบายขึ้นโดยไม่ต้องเพ่ง · ขนาดวิดเจ็ตปกติ (420px) ไม่เปลี่ยน

**แก้ไข**
- หัวข้อย่อยเคยติด CSS ของป้ายกำกับทั่วแอปมาโดยไม่ตั้งใจ ทำให้ "POS App" กลายเป็น "POS APP" ตัวพิมพ์ใหญ่ทั้งหมดและเว้นระยะห่างตัวอักษรผิดปกติ — ตอนนี้แสดงตามต้นฉบับ

## v1.4.3

**แก้ไข**
- กดสารบัญในหน้าอ่านสรุปประชุมแล้วหัวข้อที่กระโดดไปหายไป — แท็บ สรุป/ถอดเสียง/ไฟล์ ด้านบนค้างอยู่แบบ sticky ตอนเลื่อน แต่การกระโดดไม่เผื่อระยะให้ หัวข้อเลยเลื่อนไปซ้อนอยู่ใต้แท็บพอดีจนบังมิด ตอนนี้เผื่อระยะไว้แล้ว กดแล้วเห็นหัวข้อชัดเจนทุกครั้ง

## v1.4.2

**แก้ไข**
- v1.4.1 คุมความกว้างแคบไป (880px) พอขยายเต็มจอเลยเหลือที่ว่างสองข้างเยอะ เสียดายพื้นที่ — รอบนี้**ขยายเป็น 1440px และเอาพื้นที่ที่ได้ไปโชว์ของเพิ่ม ไม่ใช่แค่ยืดบรรทัดให้ยาว**
  - **Workspace** การ์ดโปรเจกต์ต่อแถวเพิ่มจาก 4 เป็น 5-6 ใบ
  - **Redmine** ชื่องานยาว ๆ ไม่โดนตัดกลางคันเหมือนเดิม
  - **Meeting** รายการประชุมเปลี่ยนเป็นกริด — จอกว้าง 3 คอลัมน์ · ~900px 2 คอลัมน์ · วิดเจ็ตปกติ 1 คอลัมน์เท่าเดิม
  - **หน้าอ่านประชุม** พอกว้างพอ **สารบัญย้ายไปเป็นแถบข้างด้านซ้ายที่ค้างตามหน้าจอ** (เลื่อนอ่านไปไหนก็กดกระโดดได้ตลอด) ไม่กินที่ด้านบนอีก · เว้นระยะระหว่างรายการให้อ่านง่ายขึ้น · ตัวหนังสือยังคุมไว้ที่ 820px ให้กวาดสายตาไหว
- หน้าตั้งค่าคุมไว้ที่ 720px (เป็นฟอร์ม ยิ่งกว้างยิ่งกรอกยาก)

## v1.4.1

**แก้ไข**
- ตอนกดขยายเต็มจอ เนื้อหาเคยยืดเต็มความกว้างหน้าต่าง อ่านยากและตัวเลขฝั่งขวา (เช่นเวลาของแต่ละประชุม) ถูกดันไปติดขอบจอจนหลุดสายตา — ตอนนี้**คุมความกว้างคอลัมน์ไว้ที่ 880px จัดกลางจอเท่ากันทั้ง Redmine / Workspace / Meeting และหน้าตั้งค่า** สลับแท็บแล้วไม่กระโดด
- ใน tab Meeting ตัวหนังสือล้วน (สรุป + บทถอดเสียง + สารบัญ) แคบลงอีกเหลือ 760px และปุ่ม สรุป/ถอดเสียง/ไฟล์ ไม่ยืดเป็นแถบยาวอีกต่อไป
- ขนาดวิดเจ็ตปกติ (420px) เหมือนเดิมทุกอย่าง กฎนี้ทำงานเฉพาะตอนหน้าต่างกว้างพอ

## v1.4.0

**เพิ่ม**
- tab **Meeting** ใหม่ — อ่านโฟลเดอร์บันทึกประชุม (1 โฟลเดอร์ = 1 ประชุม ชื่อ `YYYY-MM-DD_HH-MM-ชื่อ` ข้างในมี `summary.md` / `transcript.md` / ไฟล์เสียง)
  - รายการจัดกลุ่มตามวัน โชว์ตัวอย่างสรุป 2 บรรทัด + แท็กหัวข้อ + ป้ายผู้พูด/ประโยค/action/ขนาดไฟล์เสียง
  - กรองด้วยช่องค้นหา · ชิปวันที่ · **ชิปหมวดหัวข้อ** ที่ดึงจากหัวข้อย่อยในไฟล์สรุปให้อัตโนมัติ
  - หน้าอ่านเต็มมีแท็บ สรุป / ถอดเสียง / ไฟล์ — สรุปมี**สารบัญคลิกกระโดด** ถอดเสียงแยก**สีพื้นหลังต่อผู้พูด** กรองเฉพาะคนที่เลือกและค้นหาพร้อมไฮไลต์ได้ ไฟล์คลิกเปิดด้วยโปรแกรมของเครื่อง
  - ตั้ง path โฟลเดอร์ประชุมได้ที่ ⚙ → การ์ด Meeting · refresh เองทุก 5 นาที และมีปุ่ม ↻
  - transcript โหลดตอนกดเข้าไปอ่านทีละประชุม (รายการส่งแค่ metadata + สรุป)

## v1.3.15

**แก้ไข**
- tab **ALL** ไม่รวมงานสถานะ **Backlog** แล้ว — นับและแสดงเฉพาะงานที่กำลังทำจริง (New / In Progress / Test / Resolved) งาน Backlog ยังดูได้ที่ tab Backlog ตามเดิม

## v1.3.14

**แก้ไข**
- tab **ALL** นับและแสดงเฉพาะงานที่ยังเปิดอยู่ (ไม่รวม Closed) — เดิมรวมงานปิดด้วยทำให้ตัวเลขพองเกินจริง งานปิดยังดูได้ที่ tab Closed ตามเดิม

## v1.3.13

**เพิ่ม**
- ตัวเลขบน chip โปรเจกต์ / ผู้รับผิดชอบ และ tab ระดับความเสี่ยง แยกแสดงเป็น เปิด/ปิด (เช่น `142/49`) แทนยอดรวม — เปิดสีเขียว ปิดสีเทา (muted)

## v1.3.12

**เพิ่ม**
- การ์ด CLOSED ใน "ภาพรวม Redmine" กดเพื่อขยายดูยอดปิดงานแยกตามปีได้ (ปีล่าสุด 3 ปี รวมปีเก่ากว่านั้นไว้ใน "ก่อนหน้า")

## v1.3.11

**เพิ่ม**
- ดึงงานทุกสถานะจริงทั้งหมดจาก Redmine (ไม่จำกัดจำนวนอีกต่อไป) ตัวเลขนับทุก tab/stat tile เป็นค่าจริง 100%
- รายการงานโชว์ 15 ตัวแรก มีปุ่ม "โหลดเพิ่ม" กดดูเพิ่มทีละ 15

## v1.3.10

**เพิ่ม**
- รวมหน้าตั้งค่าไว้ที่เดียว (ปุ่มเฟือง) เป็นการ์ดแยกต่อ tab (Redmine, Workspace) ย่อ/ขยายได้ ค่าเริ่มต้นย่อไว้ รองรับเพิ่ม tab settings อื่นในอนาคต
- แบนเนอร์ "ยังไม่ได้ตั้งค่า" ของทั้ง Redmine และ Workspace กดแล้วเปิดการ์ดที่เกี่ยวข้องให้อัตโนมัติ

## v1.3.9

**เพิ่ม**
- หน้าตั้งค่า Redmine ในแอป (ปุ่มเฟืองที่แถบหัว) ใส่ URL/API key เองได้ พร้อมทดสอบการเชื่อมต่อก่อนบันทึก
- แบนเนอร์ "ยังไม่ได้ตั้งค่า Redmine" พร้อมปุ่มพาไปตั้งค่า เมื่อยังไม่เคยกรอกข้อมูล

**แก้ไข**
- ตัวติดตั้งใหม่จะไม่ฝัง `.env`/API key เข้าไปอีกแล้ว (ก่อนหน้านี้ทุกตัวติดตั้งมี API key จริงฝังอยู่)
- Workspace tab โชว์ "Coming soon" แทน error ดิบเมื่อหา vault ไม่เจอ

## v1.3.8

**เพิ่ม**
- งานที่เลยกำหนดส่ง (overdue) หัวข้องานจะขึ้นเป็นสีเหลืองอำพันให้เห็นชัด

## v1.3.7

**เพิ่ม**
- เพิ่ม tab **Closed** โชว์งานที่ปิดแล้ว (ล่าสุด 50 งาน)
- Greeting ใช้ชื่อจริงจาก Redmine แทน "Ball"
- ส่วน "ภาพรวม vault" เดิม (เลข hardcode) เปลี่ยนเป็น "ภาพรวม Redmine" แสดงจำนวนงาน open / high risk / overdue / closed จริง
- เลข `#N` หน้าแต่ละงานเปลี่ยนสีตามสถานะ ดูง่ายขึ้นตอนดูรวมกันในแท็บ ALL

## v1.3.6

**อื่นๆ**
- เวอร์ชันทดสอบ end-to-end ของ auto-update ตัวใหม่ (เช็ค/โหลด/ติดตั้งจริงจากแอปที่รันอยู่) ไม่มีการเปลี่ยนแปลงอื่น

## v1.3.5

**แก้ไข**
- แก้ auto-update เวอร์ชัน 1.3.4 ที่ path ติดตั้งมีช่องว่าง (เช่น "COWORK Desktop") จะถูกตัดครึ่งตอนติดตั้งอัตโนมัติ ทำให้ได้โฟลเดอร์ผิด — ตอนนี้ path ที่มีช่องว่างติดตั้งถูกต้องแล้ว

## v1.3.4

**เพิ่ม**
- auto-update กลับมาแล้ว เขียนใหม่เองแทนตัวเดิม (electron-updater) — เช็ค/โหลด/ติดตั้งเวอร์ชันใหม่ให้ลง path เดิมที่ติดตั้งไว้จริงเสมอ ไม่กระโดดไปติดตั้งซ้อนที่อื่นแบบที่เคยเจอ

## v1.3.3

**เพิ่ม**
- แสดงเลขเวอร์ชันที่กำลังรันอยู่ (เช่น `v1.3.3`) ข้างชื่อ COWORK บนแถบหัวของ widget

**เอาออก**
- เอา auto-update (electron-updater) ออกทั้งหมด — พบว่าเวลาอัปเดตอัตโนมัติจะติดตั้งลง path เริ่มต้นของ NSIS เสมอ ไม่สนใจ path ที่เลือกเองตอนติดตั้งด้วยมือ ทำให้เกิดแอปซ้อนกันสองชุดในเครื่อง ตอนนี้ต้องติดตั้งด้วยไฟล์ installer เองทุกครั้งที่มีเวอร์ชันใหม่

## v1.3.2

**เพิ่ม**
- ปุ่ม **ปิดงาน** สำหรับ issue ที่สถานะ Resolved ตอนนี้เปิด preview ให้ดูประวัติ (comment) ทั้งหมดของ issue เทียบกับ field **Test Results** ก่อนปิดงาน — แก้ไข Test Results ในกล่องได้โดยตรง กดยืนยันแล้วจะบันทึกทับค่าเดิมพร้อมเปลี่ยนสถานะเป็น Closed ในขั้นตอนเดียว

## v1.3.1

**แก้ไข**
- แก้ auto-update ที่เช็คอัปเดตไม่สำเร็จแบบเงียบๆ (404 จาก GitHub) — repo เปลี่ยนเป็น public แล้ว และแก้ config ให้ client รุ่นนี้เป็นต้นไปไม่ต้องใช้ token ก็เช็ค/โหลดอัปเดตได้

## v1.3.0

**เพิ่ม**
- ตัวกรองงานตาม **ระดับความเสี่ยง** (Low / Fairly Low / Moderate / Medium / High / ไม่ระบุ) ในแถบ Redmine
- ปุ่ม **ปิดงาน** สำหรับ issue ที่สถานะ Resolved — กดยืนยัน 2 ครั้งเพื่อเปลี่ยนสถานะเป็น Closed ใน Redmine โดยตรงจาก widget

## v1.2.1

**เพิ่ม**
- แถบงาน Redmine แต่ละอันตอนนี้ชี้เมาส์ที่ชื่องานจะขึ้น tooltip บอกว่า **สร้างมานานเท่าไหร่** และ **แก้ไขล่าสุดเมื่อไหร่** (เช่น "สร้าง 18 วันที่แล้ว · แก้ไข 3 ชม.ที่แล้ว")

**แก้ไข**
- เอา tooltip ซ้ำซ้อน "เปิดใน Redmine" ที่ขึ้นทับกับ tooltip เวลาออก ลดความรก
