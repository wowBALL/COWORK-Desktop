---
qtest: 1
issue: 690
round: 1
subject: "[ตัวอย่าง] login แล้ว sidebar ว่างทั้งแถบ"
project: ชื่อโปรเจกต์
tracker: Bug
receivedAt: 2026-08-06
status: done
doneAt: 2026-08-06
model: litellm/gemma4
comments: 3
---

## Checklist

| # | สิ่งที่ต้องทดสอบ | ทำโดย | ผล | วันที่ | เทส | run | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| 1 | login ด้วยบัญชี owner แล้ว sidebar แสดงเมนูครบทุกเมนู | qa | pass | 2026-08-06 |  |  |  |
| 2 | dashboard แสดงยอด outlet ไม่เป็น 0 | qa | fail | 2026-08-06 |  |  | ว่างเฉพาะ outlet ที่ไม่มี owner |
| 3 | สั่งอาหาร dine-in จนจบแล้วยอดตรง | auto | pass | 2026-07-27 | zinga-wallet-test-food-chanyathaidemo.js | 20260727173450 |  |
| 4 | ตรวจ log ฝั่ง server | ข้าม | – |  |  |  | ไม่เกี่ยวกับงานนี้ |

## บันทึกเพิ่มเติม

ไฟล์นี้เป็นตัวอย่างโครงใบเทส สร้างจาก serializeQtest() ใน testingroom.js เอง จึงไม่มีทางหลุดจากของจริง
ใบจริงเกิดจากปุ่ม 🧪 ในแท็บ Redmine (เฉพาะงานสถานะ Test) เขียนลง <qtestDir>/yyyymmdd-<เลข issue>.md
ส่งเทสซ้ำได้ทุกเมื่อ ใบใหม่จะเติม -2, -3 ต่อท้าย และ round ในหัวไฟล์นับต่อให้เอง

ความหมายของคอลัมน์

- **#** — ลำดับการทดสอบของใบนี้ ไล่ใหม่อัตโนมัติทุกครั้งที่บันทึก
- **ทำโดย** — `qa` ทดสอบเองบันทึกผลเอง · `auto` เอาผลจากชุดเทสอัตโนมัติมาเติม · `ข้าม` ข้อที่ตัดสินใจไม่ทดสอบ
- **ผล** — `–` ยังไม่ทดสอบ · `pass` · `fail`
- **วันที่** — วันที่ข้อ*นั้น*ได้ผล แยกรายข้อ เพราะใบเดียวมักเทสข้ามหลายวัน · ถอนผลแล้ววันที่ถูกล้างตาม
- **เทส** — ชื่อไฟล์เทสอัตโนมัติที่ข้อนี้ผูกไว้ ตรงกับบรรทัด `TEST:` ใน test-log.txt ของรอบรัน
- **run** — เลขโฟลเดอร์ผลรันที่เอาผลมา กดในแอปแล้วเด้งไปดู log ของรอบนั้นได้

ข้อที่ตั้งเป็น `auto` และผูกไฟล์เทสไว้ กดปุ่ม "ดึงผล auto ทั้งใบ" แล้วแอปจะไปหารอบรันล่าสุดของไฟล์นั้น
มาเติม **ผล/วันที่/run** ให้เอง — วันที่ที่ได้คือวันที่รันจริง ไม่ใช่วันที่กดดึง
รอบที่ตายกลางคัน (ไม่มีบรรทัด RESULT) จะไม่ถูกตัดสินเป็น pass/fail แต่ยังเก็บเลข run ไว้ให้ไปดู log

ใน frontmatter: `doneAt` คือวันที่ของข้อที่เทสหลังสุด โผล่เมื่อทุกข้อที่ไม่ข้ามมีผลครบแล้ว
แอปคำนวณให้เองทุกครั้งที่บันทึก แก้ในไฟล์แล้วจะถูกเขียนทับ

`comments` คือจำนวน comment ในงานที่ถูกส่งไปให้ LLM อ่านตอนร่างเช็กลิสต์ใบนี้ (0 = ไม่มี comment
หรือมีแต่เป็นการเปลี่ยน field เฉย ๆ ไม่มีข้อความ) มีไว้ให้ย้อนดูได้ว่าเช็กลิสต์ร่างจากข้อมูลเท่าไหร่
งานที่คุยกันยาวมากจะถูกตัดเฉพาะ comment เก่าสุดออก และโมเดลจะได้รับแจ้งว่าถูกตัดไปกี่อัน

แก้ด้วยมือได้ทุกช่อง — key ใน frontmatter ที่เพิ่มเองจะไม่ถูกลบทิ้งตอนแอปบันทึกทับ
